# 個人偏好
- 回答跟 commit message 請使用繁體中文；程式碼中的註解使用英文
- commit message 前綴使用 `feat:`, `refactor:`, `fix:`, `chore:` 其中之一；subject 已足夠說明時不寫內文，風格可參考 Linux 社群（但使用中文）
- 每完成一個變動就紀錄一個 commit
- 監控 PR 時，CI 皆綠燈、無合併衝突即可直接 merge，不需額外詢問，也不需等待 Copilot 審查（額度可能不足）；改為定期手動執行 code-review，不必每個 PR 都跑
